#pragma once

namespace mt
{
    void readFile(int &a, int &b, int matrix[100][100]);
    bool isProst(int x);
    bool nullsum(int a, int b, int matrix[100][100]);
    void changeMatrix(int a, int b, int matrix[100][100]);
    void OutputFile(int& a, int& b, int matrix[100][100]);
}
